allprojects {
    group 'ru.alexander'
    version '1.0-SNAPSHOT'

    apply plugin: 'java'
    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }
}

apply plugin: 'war'

dependencies {
    compile project(':phonebook-web')
}

war {
    archiveName = 'phonebook.war'
}